<Window x:Class="InventoryTracker.ShoppingListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:InventoryTracker"
        mc:Ignorable="d"
        Title="ShoppingList" Height="450" Width="800">
    <Grid Background="#FF121212">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <TextBlock FontSize="25" FontWeight="Bold" Grid.Row="0" HorizontalAlignment="Center" Margin="300,0,300,10" Foreground="White">Shopping List</TextBlock>
        <ScrollViewer Grid.Row="1" Height="300">
            <TextBlock Background="#595959" x:Name="tbShoppingList"  Text="{Binding ShoppingList}" Margin="10"></TextBlock>
        </ScrollViewer>
    </Grid>
</Window>
